--- a/src/engine/shared/config_variables.h
+++ b/src/engine/shared/config_variables.h
@@ -461,6 +461,8 @@
 
 // ESP — draw lines from a point to all players matching filters
 MACRO_CONFIG_INT(KxEsp, kx_esp, 0, 0, 1, CFGFLAG_CLIENT | CFGFLAG_SAVE, "ESP: draw lines from point A to all matching players")
+MACRO_CONFIG_INT(KxAfMode, kx_af_mode, 0, 0, 1, CFGFLAG_CLIENT | CFGFLAG_SAVE, "Avoid Freeze engine: 0=Basic brute-force 1=Blatant beam search")
+MACRO_CONFIG_INT(KxAfBeamWidth, kx_af_beam_width, 32, 5, 100, CFGFLAG_CLIENT | CFGFLAG_SAVE, "Blatant avoid: beam width (search breadth)")
 MACRO_CONFIG_INT(KxEspTeamFilter, kx_esp_team_filter, 0, 0, 2, CFGFLAG_CLIENT | CFGFLAG_SAVE, "ESP team filter: 0=both 1=war 2=my")
 MACRO_CONFIG_INT(KxEspFriendFilter, kx_esp_friend_filter, 0, 0, 2, CFGFLAG_CLIENT | CFGFLAG_SAVE, "ESP friend filter: 0=both 1=ignore 2=only")
 MACRO_CONFIG_INT(KxEspDummyFilter, kx_esp_dummy_filter, 0, 0, 2, CFGFLAG_CLIENT | CFGFLAG_SAVE, "ESP dummy filter: 0=both 1=ignore 2=only")
