--- a/src/engine/shared/config_variables.h
+++ b/src/engine/shared/config_variables.h
@@ -483,7 +483,7 @@
 // then re-plan every kx_pf_pilot_sequence_length ticks with a hard CPU budget
 // (kx_pf_pilot_cpu_ms) and emit input every tick. Follow modes aim the flow
 // field at the cursor or a player instead of the finish tiles.
-MACRO_CONFIG_INT(KxPfPilotMode, kx_pf_pilot_mode, 0, 0, 2, CFGFLAG_CLIENT | CFGFLAG_SAVE, "Pilot Bot mode: 0=Off 1=Follow cursor 2=Follow player")
+MACRO_CONFIG_INT(KxPfPilotMode, kx_pf_pilot_mode, 0, 0, 3, CFGFLAG_CLIENT | CFGFLAG_SAVE, "Pilot Bot mode: 0=Off 1=Follow cursor 2=Follow player 3=Autonomous (finish)")
 MACRO_CONFIG_INT(KxPfPilotPopulation, kx_pf_pilot_population, 399, 1, 1000, CFGFLAG_CLIENT | CFGFLAG_SAVE, "Pilot Bot: population size for the population search (more = smarter but heavier)")
 MACRO_CONFIG_INT(KxPfPilotExplorationDepth, kx_pf_pilot_exploration_depth, 13, 1, 50, CFGFLAG_CLIENT | CFGFLAG_SAVE, "Pilot Bot: exploration depth in ticks to simulate ahead per candidate")
 MACRO_CONFIG_INT(KxPfPilotTopK, kx_pf_pilot_top_k, 22, 1, 128, CFGFLAG_CLIENT | CFGFLAG_SAVE, "Pilot Bot: top-K candidates kept after each generation")
--- a/src/game/client/components/controls.cpp
+++ b/src/game/client/components/controls.cpp
@@ -538,6 +538,12 @@
 	}
 	m_VisibleAimActive = false;
 
+	// KRX-style Pilot Bot autopilot: drives the ACTIVE player (flow-field
+	// steering toward cursor / nearest player / finish). Runs before Avoid
+	// Freeze so safety overrides it. Writes direction/jump only while
+	// steering, else leaves input untouched.
+	GameClient()->m_BotNet.ProcessPilotSelf();
+
 	// ZenithTee (kinetix): Avoid Freeze bot. Its header mandates running here —
 	// at the very end of SnapInput, after the other manipulators — but nothing
 	// ever called ApplyOverride (same missing-consumer class as Fake Aim / TAS).
--- a/src/game/client/components/kinetix/clickgui.cpp
+++ b/src/game/client/components/kinetix/clickgui.cpp
@@ -383,9 +383,9 @@
         {ERowType::Slider, "Ratio", nullptr, false, false, 0.1f, 10.0f, 0.1f, 1.0f, "x", nullptr, nullptr, nullptr, 0, 0, nullptr, 0},
 };
 // v1.57: Pilot Bot — real-time navigation settings (KRX pilot-style).
-static const char *const g_PilotModeOpts[] = {"Off", "Follow cursor", "Follow player"};
+static const char *const g_PilotModeOpts[] = {"Off", "Follow cursor", "Follow player", "Autonomous"};
 static SRow g_PilotBotChildren[] = {
-        {ERowType::Dropdown, "Mode", nullptr, false, false, 0, 0, 0, 0, nullptr, nullptr, nullptr, g_PilotModeOpts, 3, 0, nullptr, 0},
+        {ERowType::Dropdown, "Mode", nullptr, false, false, 0, 0, 0, 0, nullptr, nullptr, nullptr, g_PilotModeOpts, 4, 0, nullptr, 0},
         {ERowType::Slider, "Population", nullptr, false, false, 1.0f, 1000.0f, 1.0f, 399.0f, "", nullptr, nullptr, nullptr, 0, 0, nullptr, 0},
         {ERowType::Slider, "Exploration Depth", nullptr, false, false, 1.0f, 50.0f, 1.0f, 13.0f, "", nullptr, nullptr, nullptr, 0, 0, nullptr, 0},
         {ERowType::Slider, "Top-K", nullptr, false, false, 1.0f, 128.0f, 1.0f, 22.0f, "", nullptr, nullptr, nullptr, 0, 0, nullptr, 0},
--- a/src/game/client/components/kinetix/dummies/dummy_helpers.cpp
+++ b/src/game/client/components/kinetix/dummies/dummy_helpers.cpp
@@ -18,7 +18,7 @@
 bool CBotNet::HandleNoFeatureIdle(CGameClient *pGame, CNetObj_PlayerInput *pInput, int Dummy, CBotNetDummy &State)
 {
         // Nothing to do this tick — clear inputs and return.
-        if(!g_Config.m_KxAttack && !g_Config.m_KxCopyMoves && !State.m_MacroPlaying && !State.m_PathfinderGoActive && g_Config.m_KxPfPilotMode == 0)
+        if(!g_Config.m_KxAttack && !g_Config.m_KxCopyMoves && !State.m_MacroPlaying && !State.m_PathfinderGoActive)
         {
                 ResetAndCommitInput(pGame, pInput, Dummy);
                 return true;
@@ -278,61 +278,55 @@
         return true;
 }
 
-bool CBotNet::ProcessPilotBot(CGameClient *pGame, CNetObj_PlayerInput *pInput, int Dummy, CBotNetDummy &State, int LocalID)
-{
-        // v1.57: Pilot Bot — real-time autonomous navigation (KRX pilot-style).
-        // Resolves a goal every tick (finish tiles / cursor / a player), rebuilds
-        // the A* flow field toward it every kx_pf_pilot_sequence_length ticks
-        // (bounded by a hard CPU budget), and steers the character via the flow
-        // field each tick. Returns true while active (commits input + ends tick).
+// KRX-style Pilot Bot: autopilot for the ACTIVE player (not a dummy).
+// Modes: 1=Follow cursor (own crosshair), 2=Follow player (nearest),
+// 3=Autonomous (nearest finish tile). Flow-field planning state lives in the
+// active slot's CBotNetDummy. Writes direction/jump only when steering;
+// otherwise returns without touching input (player keeps control).
+void CBotNet::ProcessPilotSelf()
+{
+        CGameClient *pGame = GameClient();
         const int PilotMode = g_Config.m_KxPfPilotMode;
-        if(PilotMode < 1 || PilotMode > 2)
-                return false;
-
-        vec2 MyPos((float)pGame->m_Snap.m_aCharacters[LocalID].m_Cur.m_X,
-                (float)pGame->m_Snap.m_aCharacters[LocalID].m_Cur.m_Y);
+        if(PilotMode < 1 || PilotMode > 3)
+                return;
+        const int ActiveD = g_Config.m_ClDummy;
+        if(ActiveD < 0 || ActiveD >= MAX_DUMMIES)
+                return;
+        const int LocalId = pGame->m_Snap.m_LocalClientId;
+        if(LocalId < 0 || !pGame->m_Snap.m_aCharacters[LocalId].m_Active)
+                return;
+        CBotNetDummy &State = m_aDummies[ActiveD];
+        CNetObj_PlayerInput *pInput = &pGame->m_Controls.m_aInputData[ActiveD];
+
+        const vec2 MyPos = pGame->m_LocalCharacterPos;
         int botTX = (int)(MyPos.x / 32.0f);
         int botTY = (int)(MyPos.y / 32.0f);
 
         MaybeReloadMapGrid(0);
         if(!m_MapGridLoaded)
-        {
-                SetDirection(pGame, Dummy, false, false);
-                pInput->m_Jump = 0;
-                pGame->m_Controls.m_aInputData[Dummy] = *pInput;
-                return true;
-        }
-
-        // ---- Resolve the goal position based on the pilot mode ----
+                return;
+
+        // ---- Resolve the goal ----
         vec2 Goal;
         bool goalValid = false;
         if(PilotMode == 1)
         {
-                // Follow cursor: steer toward the PILOT's (active dummy) crosshair
-                // in world coords. NOTE: this used to read m_aCharacters[LocalID]
-                // (the driven dummy's OWN aim), pinning the goal onto the dummy
-                // itself — it "reached" the goal instantly and stood still forever.
-                // The pilot crosshair (m_aTargetPos, maintained client-side every
-                // render) is always available, unlike server extended data.
-                const int ActiveD = g_Config.m_ClDummy;
-                if(ActiveD >= 0 && ActiveD < NUM_DUMMIES)
-                {
-                        Goal = pGame->m_Controls.m_aTargetPos[ActiveD];
-                        goalValid = true;
-                }
+                // Follow cursor: own crosshair in world coords (always available).
+                Goal = pGame->m_Controls.m_aTargetPos[ActiveD];
+                goalValid = true;
         }
         else if(PilotMode == 2)
         {
-                // Follow player: aim at the nearest active, non-self player.
+                // Follow player: nearest active, non-self player.
                 float best = 1e18f;
                 for(int i = 0; i < MAX_CLIENTS; i++)
                 {
-                        if(i == LocalID || !pGame->m_aClients[i].m_Active)
+                        if(i == LocalId || !pGame->m_aClients[i].m_Active)
                                 continue;
                         if(!(pGame->m_Snap.m_aCharacters[i].m_Active))
                                 continue;
-                        float dx = pGame->m_Snap.m_aCharacters[i].m_Cur.m_X - MyPos.x;
-                        float dy = pGame->m_Snap.m_aCharacters[i].m_Cur.m_Y - MyPos.y;
+                        float dx = (float)pGame->m_Snap.m_aCharacters[i].m_Cur.m_X - MyPos.x;
+                        float dy = (float)pGame->m_Snap.m_aCharacters[i].m_Cur.m_Y - MyPos.y;
                         float d = dx * dx + dy * dy;
                         if(d < best)
                         {
@@ -345,7 +339,7 @@
         }
         else
         {
-                // Autonomous: pick the nearest reachable finish/custom-finish tile.
+                // Autonomous: nearest finish/custom-finish tile.
                 const std::vector<vec2> &vFin = PfActiveFinishTiles();
                 float best = 1e18f;
                 for(const vec2 &f : vFin)
@@ -363,13 +357,7 @@
         }
 
         if(!goalValid)
-        {
-                // No goal this tick — hold still but stay active (goal may appear).
-                SetDirection(pGame, Dummy, false, false);
-                pInput->m_Jump = 0;
-                pGame->m_Controls.m_aInputData[Dummy] = *pInput;
-                return true;
-        }
+                return;
 
         State.m_PilotGoalPos = Goal;
         State.m_PilotGoalValid = true;
@@ -377,17 +365,12 @@
         int targetTX = (int)(Goal.x / 32.0f);
         int targetTY = (int)(Goal.y / 32.0f);
 
-        // Reached the goal tile -> stop steering this tick (mode-specific).
+        // Already on the goal tile -> don't hijack input.
         if(botTX == targetTX && botTY == targetTY)
-        {
-                SetDirection(pGame, Dummy, false, false);
-                pInput->m_Jump = 0;
-                pGame->m_Controls.m_aInputData[Dummy] = *pInput;
-                return true;
-        }
+                return;
 
         // ---- Rebuild the flow field on the sequence-length cadence ----
-        int64_t CurTick = Client()->GameTick(0);
+        int64_t CurTick = Client()->GameTick(ActiveD);
         int SeqLen = pf_clamp(g_Config.m_KxPfPilotSequenceLength, 1, 32);
         bool needPlan = (State.m_PilotLastPlanTick <= 0) || (CurTick - State.m_PilotLastPlanTick) >= SeqLen
                 || targetTX != State.m_LastTargetTX || targetTY != State.m_LastTargetTY
@@ -395,30 +378,22 @@
 
         if(needPlan && IsTileWalkable(targetTX, targetTY))
         {
-                // Hard CPU budget: only plan if within the allowed window.
-                UpdatePlayerPenalty(botTX, botTY, targetTX, targetTY, LocalID);
+                UpdatePlayerPenalty(botTX, botTY, targetTX, targetTY, LocalId);
                 ComputePathfinder(botTX, botTY, targetTX, targetTY, State);
                 State.m_PilotLastPlanTick = CurTick;
         }
         else if(!IsTileWalkable(targetTX, targetTY))
-        {
-                // Goal is not walkable (e.g. cursor over a wall) — hold but keep active.
-                SetDirection(pGame, Dummy, false, false);
-                pInput->m_Jump = 0;
-                pGame->m_Controls.m_aInputData[Dummy] = *pInput;
-                return true;
-        }
-
-        // ---- Steer toward the goal via the flow field ----
+                return;
+
+        // ---- Steer via the flow field ----
         bool left = false, right = false, jump = false;
         GetMovementFromFlow(State, left, right, jump);
 
-        SetDirection(pGame, Dummy, left, right);
+        pInput->m_Direction = right ? 1 : (left ? -1 : 0);
         pInput->m_Jump = jump ? 1 : 0;
         pInput->m_PlayerFlags |= 1;
-        pGame->m_Controls.m_aInputData[Dummy] = *pInput;
-        return true;
-}
+}
+
 
 bool CBotNet::ProcessPathfinderGo(CGameClient *pGame, CNetObj_PlayerInput *pInput, int Dummy, CBotNetDummy &State, int LocalID)
 {
--- a/src/game/client/components/kinetix/dummies/dummy_management.cpp
+++ b/src/game/client/components/kinetix/dummies/dummy_management.cpp
@@ -4,9 +4,9 @@
 bool CBotNet::IsDummyActive(int Dummy) const
 {
         // NOTE: must mirror the anyBotnetFeature gate in CBotNet::OnUpdate
-        // (kinetix.cpp) — Pilot Bot follow modes (1/2) steer dummies from
-        // ProcessDummy, so they count as active here too (mode 0 = off).
-        if(g_Config.m_KxAttack || g_Config.m_KxCopyMoves || g_Config.m_KxAtkPathfinder || g_Config.m_KxPfPilotMode >= 1)
+        // (kinetix.cpp). (Pilot Bot drives the active player from SnapInput,
+        // not dummies, so it is intentionally absent here.)
+        if(g_Config.m_KxAttack || g_Config.m_KxCopyMoves || g_Config.m_KxAtkPathfinder)
                 return true;
         if(Dummy >= 0 && Dummy < MAX_DUMMIES)
         {
--- a/src/game/client/components/kinetix/dummies/process_dummy.cpp
+++ b/src/game/client/components/kinetix/dummies/process_dummy.cpp
@@ -37,10 +37,6 @@
 
         // Mirror another client's input.  Falls through when target inactive.
         if(ProcessCopyMoves(pGame, pInput, Dummy))
-                return;
-
-        // Pilot Bot — real-time autonomous navigation (KRX pilot-style).
-        if(ProcessPilotBot(pGame, pInput, Dummy, State, LocalID))
                 return;
 
         // "Go to position" pathfinder (A* + flow-field movement toward m_PathfinderGoPos).
--- a/src/game/client/components/kinetix/kinetix.h
+++ b/src/game/client/components/kinetix/kinetix.h
@@ -473,6 +473,7 @@
 
         // Pathfinder tab (Kinetix→Pathfinder) — chunk-based mini TAS maker
         void UpdatePathfinder();            // called from OnUpdate; runs one chunk if RUNNING
+        void ProcessPilotSelf(); // KRX-style autopilot: drives the ACTIVE player (modes 1-3), called from SnapInput
         void RenderPathfinderPath();        // called from OnRender; draws m_PfVPath like RenderPackage
         void PfResetRun();                  // clear vPath + sim state (start fresh from active player)
         void PfClearSearchState();          // BUG4 v1.56.165: release A*/RHEA buffers (safe from any state)
@@ -656,7 +657,7 @@
         // Continuous loop: resolve a goal (finish / cursor / player), rebuild the
         // flow field every kx_pf_pilot_sequence_length ticks within a CPU budget,
         // and steer the character toward it every tick. Returns true when active.
-        bool ProcessPilotBot(CGameClient *pGame, CNetObj_PlayerInput *pInput, int Dummy, CBotNetDummy &State, int LocalID);
+        // (Pilot Bot moved to ProcessPilotSelf driving the active player.)
 
         // Shared A* engine for ComputePathfinder / ComputePathfinderRescue.
         // Multi-source BFS-style Dijkstra/A* from `sources` toward bot tile.
--- a/src/game/client/components/kinetix/kinetix.cpp
+++ b/src/game/client/components/kinetix/kinetix.cpp
@@ -400,12 +400,11 @@
         // Solution: skip the entire ProcessDummy loop when no botnet features are
         // active. This makes the default dummy input pipeline byte-identical to DDNet.
         // NOTE: must mirror CBotNet::IsDummyActive (dummy_management.cpp) — it
-        // treats m_KxAtkPathfinder and Pilot Bot follow modes (m_KxPfPilotMode
-        // >= 1) as active features. Omitting them here skips ProcessDummy when
-        // ONLY "Pathfinder Enabled" or Pilot follow is on, silently killing
-        // the attack-pathfinder (process_dummy.cpp uses m_KxAtkPathfinder) and
-        // the Pilot Bot (ProcessPilotBot). Mode 0 = off (see HandleNoFeatureIdle).
-        bool anyBotnetFeature = g_Config.m_KxAttack || g_Config.m_KxCopyMoves || g_Config.m_KxAtkPathfinder || g_Config.m_KxPfPilotMode >= 1;
+        // treats m_KxAtkPathfinder as an active feature. Omitting it here skips
+        // ProcessDummy when ONLY "Pathfinder Enabled" is on, silently killing
+        // the attack-pathfinder. (Pilot Bot drives the active player from
+        // SnapInput now, not dummies, so it is intentionally absent here.)
+        bool anyBotnetFeature = g_Config.m_KxAttack || g_Config.m_KxCopyMoves || g_Config.m_KxAtkPathfinder;
         if(!anyBotnetFeature)
         {
                 for(int D = 0; D < MAX_DUMMIES; D++)
